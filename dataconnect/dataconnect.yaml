specVersion: "v1"
serviceId: "admission"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "admission-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
